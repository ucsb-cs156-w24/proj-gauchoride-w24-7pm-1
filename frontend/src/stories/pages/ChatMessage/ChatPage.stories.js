import React from 'react';

import ChatPage from 'main/pages/ChatPage';

export default {
    title: 'pages/ChatMessage/ChatPage',
    component: ChatPage
};

const Template = () => <ChatPage />;

export const Default = Template.bind({});